import { TextField as MuiTextField } from '@mui/material';
import { TextFieldProps } from './types';

const TextField = (props: TextFieldProps) => {
    return <MuiTextField></MuiTextField>;
};

export default TextField;
