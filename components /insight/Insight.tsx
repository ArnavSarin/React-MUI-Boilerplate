import Box from '@mui/material/Box';
import { InsightProps } from './types';

const Insight = ({ helperText, secondaryText, primaryText }: InsightProps) => {
    return <Box></Box>;
};

export default Insight;
